spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_db
    username: sumu
    password: '""'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    consumer:
      properties:
        spring:
          json:
            trusted:
              packages: '*'
            value:
              default:
                type: sk.com.ecommerce.inventory_service.event.OrderEvent
server:
  port: 8082
